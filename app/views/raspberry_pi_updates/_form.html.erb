<%= form_with(model: raspberry_pi_update, local: true) do |form| %>
  <% if raspberry_pi_update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raspberry_pi_update.errors.count, "error") %> prohibited this raspberry_pi_update from being saved:</h2>

      <ul>
        <% raspberry_pi_update.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :version %>
    <%= form.text_field :version %>
  </div>

  <div class="field">
    <%= form.label :payload %>
    <%= form.text_area :payload %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

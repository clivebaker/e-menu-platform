    <% if sub.provenance.present? %>

                  <div class="card">
                    <div class="card-header" id="headingThree-<%=sub.id%>">
                      <h5 class="mb-0">
                        <button class="btn btn-link collapsed" type="button" data-toggle="collapse" data-target="#collapseThree-<%=sub.id%>" aria-expanded="false" aria-controls="collapseThree-<%=sub.id%>">
                          <%= t('menu.provenance')%>
                        </button>
                      </h5>
                    </div>
                    <div id="collapseThree-<%=sub.id%>" class="collapse" aria-labelledby="headingThree-<%=sub.id%>" data-parent="#accordionExample-<%=sub.id%>">
                      <div class="card-body menu-description ml-3">
                         <%= sub.provenance.html_safe if sub.provenance %>
                      </div>
                    </div>
                  </div>
                  <% end %>
<%= form_with(model: [:manager,manager_setting], local: true) do |form| %>
  <% if manager_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manager_setting.errors.count, "error") %> prohibited this manager_setting from being saved:</h2>

      <ul>
      <% manager_setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control"  %>
  </div>

   <div class="form-group">
    <%= form.label :key %>
    <%= form.text_field :key, class: "form-control" %>
  </div>

   <div class="form-group">
    <%= form.label :category %>
    <%= form.text_field :category, class: "form-control" %>
  </div>

   <div class="form-group">
    <%= form.submit nil, class: 'btn btn-primary' %>
  </div>
<% end %>

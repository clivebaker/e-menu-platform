<%= form_with(model: pi_interface, local: true) do |form| %>
  <% if pi_interface.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pi_interface.errors.count, "error") %> prohibited this pi_interface from being saved:</h2>

      <ul>
      <% pi_interface.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :server_token %>
    <%= form.text_field :server_token %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

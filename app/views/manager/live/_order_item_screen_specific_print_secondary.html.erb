
<%
items = screen_item.receipt.items['items'].select{|d| d['uuid'] == screen_item.uuid} if !grouped
items = screen_item.items['items'].select{|d| d['secondary_item_screen_type_key'] == secondary_item_screen_type_key} if grouped
  %>  

<% items.each do |item| %>
<% i = item['item'].gsub("<i>","").gsub("</i>","").gsub("<strong>","").gsub("</strong>","").split(" - ")%>
<%= i[0]%>
<%= i[1]%>
<% item['optionals'].each do |optional| %>
<%= optional.gsub("<i>","").gsub("</i>","").gsub("<strong>","").gsub("</strong>","")%>
<% end %>

<% if item['note'].present? %>
Note: <%=item['note']%>
<% end %>
-----------------------------------------
<% end %>
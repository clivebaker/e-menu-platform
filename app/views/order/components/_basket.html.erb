
        <div class="row bg-light-70">
          <div class="col"> 
            <h3>Basket</h3>
          </div>
          <div class="col text-right"> 
            <h3>Items: <%=@basket_item_count%></h3>
          </div>
        </div>
        <div class="row bg-light-70">
          <div class="col">
            <table class='table'>
              <thead>
                <tr>
                  <th></th>
                  <th>Item</th>
                  <th>Extras</th>
                  <th>Total</th>
                </tr>
              </thead>
              <tbody>
                <% @basket['items'].each do |item|%>
                <%
=begin%>
 <%binding.pry%> 
<%
=end%>
                <% note = item['note'] %>
                <tr>
                  <td><%= link_to remove_from_basket_path(@path, item['uuid'], menu_id: params[:menu_id], section_id: params[:section_id]), data: { confirm: 'Are you sure?' }  do %><i class="fas fa-minus-circle text-danger"></i><% end %></td>
                  <td><%= item['item'].html_safe  %>
                    <%if note.present?%><br>
                      <strong>Note:</strong> <%= note.html_safe%><% end %></td>
                  <td><%= item['optionals'].join('<br> ').html_safe %></td>
                  <td class='text-right'><%= number_to_currency(item['total'], unit: '£') %></td>
                </tr>
                <% end if @basket %>
                <tr>
                  <td>Total</td>
                  <td></td>
                  <td></td>
                  <td class='text-right'><%= number_to_currency(@basket_item_total, unit: '£') %></td>
                </tr>
              </tbody>  
            </table> 
          </div>
        </div>
        <div class="row pb-2 bg-light-70">
          <div class="col"> 
          
          <% 
          disable_basket_link = true 
          if @basket_item_count > 0
            disable_basket_link = false 
          end
          %>
            <%= button_to "CHECKOUT", checkout_path(@path), disabled: disable_basket_link, method: :get,  class: 'btn btn-dark btn-block'%>
          </div>
        </div>


 